if (DEBUG_UT STREQUAL "ON")
    add_subdirectory(mxm-unit-tests)
    add_subdirectory(3rdparty)
elseif (DEBUG_FUZZ STREQUAL "ON")
    add_subdirectory(fuzz)
else ()
    add_subdirectory(tools)
endif ()